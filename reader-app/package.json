{
  "name": "reader-app",
  "private": true,
  "description": "Novel Reader - Auto-update trigger",
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "sync": "python3 ../scripts/prepare_reader_data.py",
    "dev": "npm run sync && vite",
    "build": "npm run sync && vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "vite": "^7.3.1"
  }
}